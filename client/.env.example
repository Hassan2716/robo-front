# Frontend Environment Variables
# Copy to .env for local development

# API URL - In production, set this in Vercel project settings
# Local dev: leave unset (Vite proxy handles /api -> localhost:5000)
# Production: https://robo-back-1.onrender.com/api
VITE_API_URL=https://robo-back-1.onrender.com/api
